A p:with-param on a call to a user-defined pipeline is always used,
irrespective of its placement with respect to any parameter input
ports defined on the step.

On this call:

<ex:something>
  <p:with-param name="param" select="'local value'">
    <p:empty/>
  </p:with-param>
  <p:input port="parmin">
    <p:inline>
      <c:param-set>
	<c:param name="pname" value="book"/>
	<c:param name="param" value="global value"/>
      </c:param-set>
    </p:inline>
  </p:input>
</ex:something>

The value of param will be 'local value' even though 'global value'
should be the result.
